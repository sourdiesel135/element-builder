<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- TinyAmp Neural - Cyberglass Theme -->
    <style name="Theme.TinyAmp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/neon_cyan</item>
        <item name="colorPrimaryVariant">@color/cyan_glow</item>
        <item name="colorOnPrimary">@color/glass_darker</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/neon_pink</item>
        <item name="colorSecondaryVariant">@color/pink_glow</item>
        <item name="colorOnSecondary">@color/text_primary</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/background_primary</item>
        <item name="colorSurface">@color/glass_surface</item>
        <item name="colorOnBackground">@color/text_primary</item>
        <item name="colorOnSurface">@color/text_primary</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowTranslucentStatus">true</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/transparent</item>
        <item name="android:windowTranslucentNavigation">true</item>

        <!-- Window properties -->
        <item name="android:windowBackground">@color/background_primary</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Glass Card Style -->
    <style name="GlassCard">
        <item name="android:background">@drawable/glass_card_background</item>
        <item name="android:padding">16dp</item>
        <item name="android:elevation">8dp</item>
    </style>

    <!-- Neon Button Style -->
    <style name="NeonButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/neon_cyan</item>
        <item name="backgroundTint">@color/transparent</item>
        <item name="strokeColor">@color/neon_cyan</item>
        <item name="strokeWidth">2dp</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <!-- Neon Text Style -->
    <style name="NeonText">
        <item name="android:textColor">@color/neon_cyan</item>
        <item name="android:shadowColor">@color/cyan_glow</item>
        <item name="android:shadowDx">0</item>
        <item name="android:shadowDy">0</item>
        <item name="android:shadowRadius">8</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <!-- Consciousness Text Style -->
    <style name="ConsciousnessText" parent="NeonText">
        <item name="android:textColor">@color/neon_pink</item>
        <item name="android:shadowColor">@color/pink_glow</item>
    </style>
</resources>
